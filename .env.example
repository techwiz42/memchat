# Memchat Configuration
# Copy to .env and fill in values. Secrets should go in ./secrets/ files instead.

# Public hostname for Omnia tool callbacks (no trailing slash)
PUBLIC_BASE_URL=https://memchat.yourdomain.com

# Omnia voice settings
OMNIA_VOICE_NAME=Mark
OMNIA_LANGUAGE_CODE=en

# LLM model for chat
LLM_MODEL=gpt-4o

# Embedding model
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# Postgres (non-secret parts)
POSTGRES_USER=memchat
POSTGRES_DB=memchat

# Redis
REDIS_URL=redis://redis:6379/0

# Docker secrets (create these files in ./secrets/):
#   secrets/omnia_api_key       - Omnia Voice API key
#   secrets/llm_api_key         - OpenAI API key for chat LLM
#   secrets/embedding_api_key   - OpenAI API key for embeddings (can be same as llm_api_key)
#   secrets/postgres_password   - PostgreSQL password
#   secrets/app_secret_key      - JWT signing secret (generate with: openssl rand -hex 32)
