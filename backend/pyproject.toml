[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: requires PostgreSQL + pgvector (deselect with '-m not integration')",
]

[tool.coverage.run]
source = ["."]
omit = ["tests/*", "*/test_*"]
